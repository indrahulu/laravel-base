services:
  app:
    image: indrahulu/laravel-base:php8.5
    ports:
      - "${PUBLISHED_HTTP_PORT:-8080}:8080"
      - "${PUBLISHED_HTTPS_PORT:-8443}:8443"
    environment:
      APP_ROLE: all
      APP_UID: "${APP_UID:-}"
      APP_GID: "${APP_GID:-}"
      TZ: "${TZ:-Asia/Jakarta}"
      RUN_MIGRATIONS_ON_BOOT: "true"
    depends_on:
      db:
        condition: service_healthy
      frontend-build:
        condition: service_completed_successfully
      composer-install:
        condition: service_completed_successfully
    volumes:
      - ".:/var/www/html"
    restart: unless-stopped

  composer-install:
    image: indrahulu/laravel-base:php8.5
    working_dir: /var/www/html
    entrypoint: ["sh", "-c"]
    command: ["git config --global safe.directory /var/www/html && exec composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader"]
    environment:
      TZ: "${TZ:-Asia/Jakarta}"
    volumes:
      - ".:/var/www/html"
    restart: "no"
    deploy:
      resources:
        limits:
          cpus: "0.75"
          memory: 1G

  frontend-build:
    image: node:22-alpine
    environment:
      TZ: "${TZ:-Asia/Jakarta}"
    working_dir: /var/www/html
    volumes:
      - ".:/var/www/html"
    command: sh -c "rm -f public/hot && npm ci && npm run build"
    restart: "no"
    deploy:
      resources:
        limits:
          cpus: "0.75"
          memory: 1G

  db:
    image: postgres:17-alpine
    environment:
      TZ: "${TZ:-Asia/Jakarta}"
      POSTGRES_DB: "${DB_DATABASE:?DB_DATABASE is required}"
      POSTGRES_USER: "${DB_USERNAME:?DB_USERNAME is required}"
      POSTGRES_PASSWORD: "${DB_PASSWORD:?DB_PASSWORD is required}"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - db:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  db:
